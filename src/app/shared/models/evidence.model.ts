
export interface Evidence {
  id: string;
  version?: string;
  title: string;
  shortTitle?: string;
  status: string;
  publisher?: string;
  description?: string;
  exposureBackground: string;
  visited?: boolean;
}
